{% extends "catalog/base.html" %}

{% block site_wrapper %}
{% load staticfiles %}

<!-- Title -->
        <div class="row">
            <div class="col-lg-12">
                <h3>Latest Products</h3>
            </div>
        </div>
        <!-- /.row -->

<!-- Page Features -->
        <div class="row text-center">
            {% for p in products %}
                {% if p.is_featured %}
                    <div class="col-md-3 col-sm-6">
                        <div class="thumbnail">
                            <img  class="img-thumbnail img-responsive" src="{% static 'catalog/images/' %}{{ p.image }}" alt="">
                            <div class="caption">
                                <h3>{{ p.name }}</h3>
                                <p>Brand: {{ p.brand }}</p>
                                <p>
                                    <!--
                                    <a href="#" class="btn btn-primary">Buy Now!</a>
                                    --> 
                                    <a href="product/{{ p.slug }}" class="btn btn-default">More Info</a>
                                </p>
                            </div>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
        <!-- /.row -->

    	<div class="row">
		    <div class="col-lg-12">
		        <h3>All Products</h3>
		    </div>
		</div>
<!-- /.row -->

<!-- Page Features -->
        <div class="row text-center">
            {% for p in products %}
            	<div class="col-md-3 col-sm-6">
					<div class="thumbnail">
					  <img class="img-thumbnail img-responsive" src="{% static 'catalog/images/' %}{{ p.image }}" alt="Pulpit Rock" style="width:300px;height:200px">
					  <h3>{{ p.name }}</h3>
		              <p>Brand: {{ p.brand }}</p>
		              <p>
		              <!--
		              	<a href="#" class="btn btn-primary">Buy Now!</a>
		              -->
		              	<a href="product/{{ p.slug }}" class="btn btn-default">More Info</a>
		              </p>
					</div>
			  </div>
            {% endfor %}
        </div>
        <!-- /.row -->

{% endblock %}



